import fs from 'node:fs';

/** Re-export of `fs.createWriteStream` for creating writable file streams. */
export const {createWriteStream} = fs;
/** Re-export of `fs.createReadStream` for creating readable file streams. */
export const {createReadStream} = fs;
